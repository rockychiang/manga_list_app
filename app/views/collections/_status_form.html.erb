<% if user == current_user %>
  <%= form_for [user, collection] do |f| %>

    <%= f.select :status, ["Never Read", "Reading", "Finished"], {},
      value: collection.status, class: "form-control", onchange: 'this.form.submit();' %>

  <% end %>

<% else %>
  <%= collection.status %>
<% end %>
