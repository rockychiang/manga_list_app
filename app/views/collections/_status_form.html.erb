<% if user == current_user %>
  <%= form_for [user, collection] do |f| %>

    <%= f.select :status, ["Never Read", "Reading", "Finished"], {},
      value: collection.status, class: "form-control changeStatus" %>

  <% end %>

<% else %>
  <%= collection.status %>
<% end %>
